return {
    ensure_installed = {
        "c",
        "lua",
        "rust",
        "scala",
        "haskell",
        "toml",
        "glsl"
    },
    sync_install = false,

    highlight = {
        enable = true,
    },
    rainbow = {
        enable = true,
        extended_mode = true,
    }
}
